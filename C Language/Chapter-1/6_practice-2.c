#include <stdio.h>

int main()
{
    int radius = 3;
    float pi = 3.14;
    printf("The Area of Cricle is %f \n", pi * radius * radius );

    int height = 2;
    printf("The Area of Cylinder is %f \n", radius * radius * pi * height);

    return 0;
}